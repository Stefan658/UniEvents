PORT=5000
FLASK_DEBUG=True

SECRET_KEY=super-secret-key
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/uni_events

POSTGRES_DB=uni_events
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_HOST=db
POSTGRES_PORT=5432

# Email configuration (SMTP)
# If these variables are missing, email sending will be safely skipped.
# For Gmail: Use an "App Password", not your regular account password.
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=
SMTP_PASSWORD=
SMTP_FROM_EMAIL=noreply@uni-events.ro
SMTP_USE_TLS=True

# For local fake SMTP testing with `python -m smtpd -c DebuggingServer -n localhost:1025`, 
# set SMTP_AUTH_REQUIRED=False and SMTP_USE_TLS=False.
SMTP_AUTH_REQUIRED=True